# Shared contracts

Cross-platform definitions consumed by iOS and backend.

| Path | Purpose |
|------|---------|
| `api/openapi.yaml` | REST API contract (stub) |
| `schemas/program-manifest.schema.json` | Practice program bundle format |
| `schemas/` | DynamoDB entity shapes (JSON Schema) |

Keep versions in manifests and OpenAPI `info.version` when breaking changes occur.
