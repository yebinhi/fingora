# Fingora CDK

Deploy with AWS credentials configured:

```bash
npm install
npx cdk synth
```

Stacks (stubs in `lib/`): `auth-stack`, `data-stack`, `api-stack`, `processing-stack`. Wired from `FingoraStage` in a follow-up change.
