# @fingora/shared

Shared TypeScript: entity types, DynamoDB key helpers, validation against `shared/schemas/`.

To be published as a local workspace package consumed by `services/api` and `services/workers`.
